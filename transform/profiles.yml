personal_finance:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', '../data/finance.duckdb') }}"
      threads: 4
    ci:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', '../data/finance.duckdb') }}"
      threads: 2
    prod:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', '../data/finance.duckdb') }}"
      threads: 4
